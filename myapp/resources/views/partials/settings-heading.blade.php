<div class="relative mb-10 w-full overflow-hidden border border-white/60 bg-white/85 p-6 shadow-[0_18px_48px_rgba(50,97,171,0.12)] backdrop-blur-lg">
    <span class="brand-tag mb-4">{{ __('Profile Center') }}</span>
    <flux:heading size="xl" level="1" class="text-3xl text-slate-800">{{ __('Settings') }}</flux:heading>
    <flux:subheading size="lg" class="mt-2 max-w-2xl text-slate-500">{{ __('Manage your profile and account settings') }}</flux:subheading>
</div>
